{
  "name": "lista-auto-healing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/catalog.js && node --check src/catalog_state.js && node --check src/parsers.js && node --check src/failover.js && node --check src/hls.js && node --check src/health.js && node --check src/providers/pluto.js && node --check scripts/refresh-catalog.mjs && wrangler deploy --dry-run --outdir .wrangler-dist",
    "refresh:catalog": "node scripts/refresh-catalog.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
