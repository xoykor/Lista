{
  "name": "lista-auto-healing",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/build.js && node --check src/artwork.js && node --check src/health.js && node --check src/taxonomy.js && node --check src/normalize.js && node --check src/restricted.js && node --check scripts/build-catalog.mjs",
    "build": "node scripts/build-catalog.mjs"
  }
}
